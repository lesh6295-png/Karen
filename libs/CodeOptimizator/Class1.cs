using System;
using System.Text;
namespace Karen.CodeOptimizator
{
    public static class CodeOptimizator
    {
        public static string Process(string script)
        {
            var q = script.Split("\n");
            StringBuilder finalscript = new();






            return finalscript.ToString();
        }
    }
}
